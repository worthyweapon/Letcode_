package practice;

/*
 * close() = The browser will get closed but it will be active in the memory. 
 * 					Can be checked in Task manager(chromedriver.exe will be running in task manager
 * 			 If 2 tabs are open, close() will close only the current tab
 * 
 * quit() = Closes the browser and leaves the memory. 
 * 
 * 			If 2 tabs are open, quit() will close the browser. It will be used 
 * every time unless there is an use for close()	
 */
